# .secret.example
#
# 此文件用于说明 gradio_app/.secret 中应包含的配置项。
# 请将此文件重命名为 .secret 并根据您的实际环境填写配置值。
# .secret 文件不应提交到版本控制。

# 新增：WebGW 互联网 API 端点
WEB_GW_API_URL="gw-api-url"

# 新增：WebGW 互联网 API 的密钥
WEB_GW_API_KEY="your-webgw-api-key"

# 新增：WebGW App ID
WEB_GW_APP_ID="your-webgw-app-id"

# Gradio 应用监听的主机地址。
# - "127.0.0.1" 或 "localhost" 仅允许本地访问。
# - "0.0.0.0" 允许从任何网络接口访问 (适用于部署到服务器)。
GRADIO_APP_HOST="127.0.0.1"

# Gradio 应用监听的端口。
# 请确保此端口未被其他应用程序占用。
GRADIO_APP_PORT="7860"

# 是否将请求的详细信息（URL、Payload、Headers）记录到日志。
# - true: 开启记录
# - false: 关闭记录
DUMP_REQS="true"

# 仅可使用 false
USE_INTRANET_API="false"
